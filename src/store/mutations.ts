export default {
  // DELETE_USER(state: { users: []; }, id: number){
  //   const index = state.users.findIndex((user: { id: number; }) => user.id == id);
  //   state.users.splice(index, 1);
  // },
  // CREATE_USER(state: { users: any; }, user: []){
  //  state.users.push(user);
  // },
  // EDIT_USER(state: { users: any; }, user){
  //     const items = state.users
  //     const foundIndex = items.findIndex((x) => x.id == user.id)
  //     items[foundIndex] = user
  // }
};
