export default {
    postsList(state: { postsList: [] }) {
    return state.postsList;
    },

    postsTrash(state: { postsTrash: [] }) {
    return state.postsTrash;
    },

    postDetail(state: { postDetail: any }) {
    return state.postDetail;
    },

    postsTop4(state: { postsTop4: any }) {
    return state.postsTop4;
    },
};
